cloud:
  aws:
    credentials:
      access-key: AKIA3N55WRHMA64FCAOG
      
    region:
      static: ap-south-1
    stack:
      auto: false

application:
  bucket:
    name: pabitras3bucket2024

spring:
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2MB
      max-file-size: 5MB
      max-request-size: 10MB

server:
  port: 8080

